name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  workspace:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Format
        run: cargo fmt --all -- --check
      - name: Check workspace
        run: cargo check --workspace --all-targets
      - name: Clippy workspace
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Workspace tests
        run: cargo test --workspace --all-targets
      - name: Bandit tests
        run: cargo test -p forge-core --lib --features bandit
      - name: Clippy LLM + bandit paths
        run: cargo clippy -p forge-core --all-targets --features bandit -- -D warnings

  nnis-adapter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Format NNIS adapter
        run: cargo fmt --manifest-path adapters/forge-nnis/Cargo.toml --all -- --check
      - name: Check NNIS adapter
        run: cargo check --manifest-path adapters/forge-nnis/Cargo.toml --all-targets
      - name: Clippy NNIS adapter
        run: cargo clippy --manifest-path adapters/forge-nnis/Cargo.toml --all-targets -- -D warnings
      - name: Test NNIS adapter host gates
        run: cargo test --manifest-path adapters/forge-nnis/Cargo.toml --all-targets
